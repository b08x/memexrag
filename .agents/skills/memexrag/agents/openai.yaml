interface:
  display_name: "Memexrag"
  short_description: "Repo-specific patterns and workflows for memexrag"
  default_prompt: "Use the memexrag repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true